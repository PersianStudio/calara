# Changelog

## 0.2.0

- Remove MUI, Emotion, i18n, react-hook-form, and design-system primitives.
- Rebuild calendar / date-picker UI with plain HTML + CSS (functionality first).
- Keep `date-fns`, `moment`, and `react-datepicker`.
- `react-icons` is showcase-only (devDependency).

## 0.1.0

- Initial extraction from ICE design system.
